<template>
  <view>
    <text>issue-26943：{{testStatus26943}}</text>
  </view>
</template>

<script setup lang="uts">
import { testGenericCallback } from '@/uni_modules/issue-26943'

const testStatus26943 = ref("测试未通过")

onLoad(() => {
  testGenericCallback({
    success(res) {
      testStatus26943.value = JSON.stringify(res) == JSON.stringify(["a", "b"]) ? "测试通过" : "测试未通过"
    }
  })
})
</script>

<style>

</style>