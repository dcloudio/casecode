<template>
  <view class="mask">
    <view class="dialog">
      <text class="title">dialogPage navigationBarTextStyle</text>
      <text class="status">navigationBarTextStyle: black</text>
      <button class="button" @click="closeDialog">close dialogPage</button>
    </view>
  </view>
</template>

<script setup lang="uts">
  const currentPage = getCurrentInstance()!.proxy!.$page

  function closeDialog() {
    uni.closeDialogPage({
      dialogPage: currentPage,
      animationType: 'fade-out',
      animationDuration: 200
    })
  }
</script>

<style>
  .mask {
    width: 100%;
    height: 100%;
    justify-content: center;
    align-items: center;
    background-color: rgba(0, 0, 0, 0.62);
  }

  .dialog {
    width: 300px;
    padding: 20px;
    background-color: #ffffff;
    border-radius: 8px;
  }

  .title {
    font-size: 20px;
    font-weight: bold;
    color: #111827;
    margin-bottom: 14px;
  }

  .status {
    font-size: 16px;
    color: #374151;
    margin-bottom: 18px;
  }

  .button {
    margin-top: 10px;
  }
</style>
