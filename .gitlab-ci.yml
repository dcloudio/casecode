image: node:22

stages:
  - review
  - uni_autotest

variables:
  GITLAB_TOKEN: $GITLAB_ACCESS_TOKEN
  DEEPSEEK_API_KEY: $DEEPSEEK_API_KEY
  GIT_STRATEGY: fetch
  CODE_REVIEW_REPO: "http://git.dcloud.io/web_team/uni-ai-code-review.git"
  CODE_REVIEW_BRANCH: "main"

code_review:
  stage: review
  script:
    # 克隆评审代码仓库到项目目录的子目录
    - echo "从远程仓库拉取评审代码..."
    - git clone --depth=1 --branch=${CODE_REVIEW_BRANCH} ${CODE_REVIEW_REPO} ${CI_PROJECT_DIR}/.ai-review-code
    # 进入评审代码目录并执行评审
    - cd ${CI_PROJECT_DIR}/.ai-review-code/code-review
    - node index.js
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH != "main" && $CI_COMMIT_BRANCH != "master"

uni_autotest:
  stage: uni_autotest
  only:
    - dev
  script:
    - |
      echo "Sending webhook to $UNI_AUTOTEST_API_URL..."
      COMMIT_MESSAGE_ESCAPED=$(printf '%s' "$CI_COMMIT_MESSAGE" | sed 's/\\/\\\\/g; s/"/\\"/g; s/$/\\n/' | tr -d '\n' | sed 's/\\n$//')
      COMMIT_AUTHOR_ESCAPED=$(printf '%s' "$CI_COMMIT_AUTHOR" | sed 's/\\/\\\\/g; s/"/\\"/g')
      curl -sS -X POST "$UNI_AUTOTEST_API_URL" \
        -H "Content-Type: application/json" \
        -d "{
          \"repo\": \"$CI_PROJECT_PATH\",
          \"project_id\": \"$CI_PROJECT_ID\",
          \"project_name\": \"$CI_PROJECT_NAME\",
          \"branch\": \"$CI_COMMIT_BRANCH\",
          \"commit_sha\": \"$CI_COMMIT_SHA\",
          \"commit_url\": \"$CI_PROJECT_URL/-/commit/$CI_COMMIT_SHA\",
          \"commit_author\": \"$COMMIT_AUTHOR_ESCAPED\",
          \"commit_timestamp\": \"$CI_COMMIT_TIMESTAMP\",
          \"commit_message\": \"$COMMIT_MESSAGE_ESCAPED\",
          \"pipeline_id\": \"$CI_PIPELINE_ID\"
        }"
